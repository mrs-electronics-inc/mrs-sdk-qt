# Demo Projects

These demo projects are meant to showcase the feature set of the MRS SDK. They are designed to work with any SDK version.

To test out a demo project with a specific SDK version:

```bash
cd <project> && mrs-sdk-manager use <sdk-version>
```
